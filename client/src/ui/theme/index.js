import { createMuiTheme } from '@material-ui/core/styles';

  const palette = {
    primary: { main: '#8ed1b7', contrastText: '#000000' },
    secondary: { main: '#ad9c78', contrastText: '#000000' }
  };
  const themeName = 'Vista Blue Sandrift Pilchard';

  export default createMuiTheme({ palette, themeName });